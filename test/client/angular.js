var assert = chai.assert;
var should = chai.should();
var expect = chai.expect;

describe('Client-side Angular', function() {
  beforeEach(module('glitch'));

  describe('Video Display', function() {
    it('should display videos', function() {
    });
  });
  describe('Song History', function() {
    it('should add videos to the playlist', function() {
    });
    it('should remove videos once the playlist is full', function() {
    });
  });
  describe('Chat Messages', function() {
    it('should add messages to the chat history', function() {
    });
  });
});
